attezt
======

Attezt is an open-source server for supporting device-attest-01 challenges for
Linux devices.

* `attezt-ca` implements the server component of `device-attest-01`
* `attezt` implements the client side command line utility for requesting and generating certificates.

# Non-goals
===========

* Windows suppport
